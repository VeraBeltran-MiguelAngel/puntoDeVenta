import { ListarCategoriaPipe } from './listar-categoria.pipe';

describe('ListarCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new ListarCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
