import { ListarProveedorPipe } from './listar-proveedor.pipe';

describe('ListarProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new ListarProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
