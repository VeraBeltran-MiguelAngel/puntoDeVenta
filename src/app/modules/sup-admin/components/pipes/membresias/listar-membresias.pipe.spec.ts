import { ListarMembresiasPipe } from './listar-membresias.pipe';

describe('ListarMembresiasPipe', () => {
  it('create an instance', () => {
    const pipe = new ListarMembresiasPipe();
    expect(pipe).toBeTruthy();
  });
});
