import { ListarProductoPipe } from './listar-producto.pipe';

describe('ListarProductoPipe', () => {
  it('create an instance', () => {
    const pipe = new ListarProductoPipe();
    expect(pipe).toBeTruthy();
  });
});
