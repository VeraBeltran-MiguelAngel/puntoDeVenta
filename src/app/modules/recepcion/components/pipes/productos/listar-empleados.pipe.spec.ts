import { ListarProductosPipe } from "./lista-proveedor.pipe";

describe('ListarMembresiaPipe', () => {
  it('create an instance', () => {
    const pipe = new ListarProductosPipe();
    expect(pipe).toBeTruthy();
  });
});